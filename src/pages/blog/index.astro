---
import Base from '../../layouts/Base.astro';
import { getCollection } from 'astro:content';

const base = import.meta.env.BASE_URL;
const articulos = (await getCollection('blog', ({ data }) => !data.borrador))
  .sort((a, b) => b.data.fecha.getTime() - a.data.fecha.getTime());

const fmt = (d) => d.toLocaleDateString('es-ES', { day: 'numeric', month: 'long', year: 'numeric' });
---

<Base
  titulo="Blog — Florín Todor"
  descripcion="Artículos sobre infraestructura, ciberseguridad e IA aplicada: fallos reales, cómo se diagnostican y qué se aprende de ellos."
  conLateral={false}
>
  <div style="max-width:820px;margin:0 auto">
    <header class="cabecera-proyecto">
      <div class="etiqueta-seccion" style="color:var(--verde);font-family:var(--mono);font-size:.82rem;text-transform:uppercase;letter-spacing:.12em;margin-bottom:.8rem">Blog</div>
      <h1>Cosas que se rompen y por qué</h1>
      <p class="resumen">
        Notas sobre infraestructura, seguridad e IA aplicada. Fallos reales que me he
        encontrado, cómo los diagnostiqué y qué me llevé de cada uno.
      </p>
      <p style="margin-top:1rem">
        <a href={`${base}rss.xml`} style="font-size:.88rem">Suscribirse por RSS →</a>
      </p>
    </header>

    {articulos.length === 0 && (
      <p style="color:var(--texto-suave)">Todavía no hay artículos publicados.</p>
    )}

    {articulos.map((a) => (
      <a class="tarjeta" href={`${base}blog/${a.id}/`} style="display:block;margin-bottom:1rem;text-decoration:none;color:inherit">
        <div style="display:flex;gap:.8rem;align-items:baseline;flex-wrap:wrap;margin-bottom:.5rem">
          <span class="pastilla-fecha" style="margin:0">{fmt(a.data.fecha)}</span>
          {a.data.minutos && <span style="color:var(--texto-tenue);font-size:.82rem">{a.data.minutos} min de lectura</span>}
        </div>
        <h2 style="font-size:1.25rem;margin-bottom:.5rem">{a.data.titulo}</h2>
        <p style="color:var(--texto-suave);font-size:.93rem;margin-bottom:.8rem">{a.data.descripcion}</p>
        <div class="etiquetas">
          {a.data.etiquetas.map((e) => <span class="etiqueta">{e}</span>)}
        </div>
      </a>
    ))}
  </div>
</Base>
